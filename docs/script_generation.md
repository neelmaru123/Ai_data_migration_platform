# Script Generation Documentation

The `script_generator` package (`apps/api/packages/script_generator`) builds self-contained ZIP migration packages containing `run.py`, `migration_plan.json`, `requirements.txt`, and setup documentation for local execution.
